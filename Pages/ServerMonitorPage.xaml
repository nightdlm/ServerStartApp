<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfAppAi.Pages"
      xmlns:ss="clr-namespace:WpfAppAi.Model"
      xmlns:hc="https://handyorg.github.io/handycontrol" x:Class="WpfAppAi.Pages.ServerMonitorPage"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ServerMonitorPage">
    <Page.DataContext>
        <ss:ServerInfoItems/>
    </Page.DataContext>

    <Grid>
        <DataGrid x:Name="ServerDataGrid"
                  ItemsSource="{Binding ServerInfoItemList}"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  hc:DataGridAttach.ShowRowNumber="True"
                  GridLinesVisibility="All"
                  HorizontalGridLinesBrush="#FFE26D6D"
                  VerticalGridLinesBrush="#FF714444" 
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="服务名称" Binding="{Binding ServerName}" Width="*"/>
                <DataGridTextColumn Header="启动端口" Binding="{Binding Port}" Width="*"/>
                <DataGridTemplateColumn Header="操作" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="启动"
                                        Tag="{Binding}"
                                        Cursor="Hand" 
                                        Click="StartButton_Click"
                                        Margin="2"
                                        Padding="5,0"
                                        Background="LightGreen"/>
                                <Button Content="停止" 
                                        Tag="{Binding}"
                                        Click="StopButton_Click"
                                        Cursor="Hand" 
                                        Margin="2"
                                        Padding="5,0"
                                        Background="LightCoral"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>