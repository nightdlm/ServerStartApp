<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppAi"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        x:Class="WpfAppAi.MainWindow"
        mc:Ignorable="d"
        Loaded="MainWindow_Loaded"
        Title="服务管理AI" Height="450" Width="800" MinHeight="450" MinWidth="800" Icon="/Resources/logo.png" Background="White" HorizontalAlignment="Center" VerticalAlignment="Center" WindowStartupLocation="CenterScreen">
    <Grid>
        <!-- 定义两列布局 -->
        <Grid.ColumnDefinitions>
            <!-- 左侧Menu列：固定宽度200 -->
            <ColumnDefinition Width="80"/>
            <!-- 右侧Frame列：占据剩余空间 -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <Grid>
            <Grid.Resources>
                <Style x:Key="MenuItemStyle" TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="Padding" Value="-10,5,0,0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Height" Value="30"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="LightBlue"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Grid.Resources>

            <Grid.RowDefinitions>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
            </Grid.RowDefinitions>

            <MenuItem x:Name="ServiceManagementMenu" Grid.Row="1" Cursor="Hand" Header="服务管理" Click="ServiceManagement_Click" Style="{StaticResource MenuItemStyle}"/>
            <MenuItem x:Name="ServiceStatusMenu" Grid.Row="2" Cursor="Hand" Header="服务状态" Click="ServiceStatus_Click" Style="{StaticResource MenuItemStyle}"/>
            <MenuItem x:Name="SystemSettingsMenu" Grid.Row="4" Cursor="Hand" Header="系统设置" Click="SystemSettings_Click" Style="{StaticResource MenuItemStyle}"/>
        </Grid>

        <!-- 右侧Frame容器 -->
        <Frame Grid.Column="1" x:Name="MainFrame" 
               NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
